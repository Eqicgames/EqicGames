/**
 * Asset-to-Asset (A2A) Transfer Module
 * Handles cross-platform asset transfers for Eqic Game platform
 */

const A2ATransfer = require('./transfer');
const A2APlatforms = require('./platforms');

module.exports = {
  A2ATransfer,
  A2APlatforms
}; 